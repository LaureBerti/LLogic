# Environments
.venv/
venv/
__pycache__/
*.pyc

# Secrets — never commit
.env
*.key

# Large ontology files (download separately — see data/ontologies/README.md)
data/ontologies/*.owl
data/ontologies/*.rdf
data/ontologies/*.ttl
data/ontologies/*.nt

# Experiment outputs (regenerated by running the pipeline)
outputs/results/
logs/

# OS / editor
.DS_Store
*.swp
